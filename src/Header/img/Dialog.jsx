import React from "react";
import p from '../../Dialog/Dialog.module.css';
import {BrowserRouter, Route} from "react-router-dom";

const DialogItem = (props) =>
{
    return

}

const Dialog = () => {
    return (
        <BrowserRouter>
            <div className={p.item}>
                <div>
                    Dialog
                </div>
            </div>
        </BrowserRouter>

    )
}
export default Dialog;